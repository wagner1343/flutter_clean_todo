name: flutter_clean_todo
description: Flutter Todo app with clean architecture

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_auth: ^4.2.1
  cloud_firestore: ^4.2.0
  get: ^4.6.5
  flutter_stream_listener: ^2.0.0
  get_it: ^7.2.0
  provider: ^6.0.5
  firebase_core: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true